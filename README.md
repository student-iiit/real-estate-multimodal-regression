# real-estate-multimodal-regression
Multimodal regression model using tabular data and satellite imagery

## Satellite Imagery Data

Satellite images are programmatically collected using the Mapbox Static Images API
based on latitude and longitude coordinates. Images are stored locally and aligned
with tabular data using property IDs.
